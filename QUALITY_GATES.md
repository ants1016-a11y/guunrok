# QUALITY_GATES — 테스트·완료 기준

모든 구현 작업(Stage 1 이후)과 완료 보고는 아래 기준을 통과해야 한다.
제안서의 "완료 조건·검증 방법"도 이 문서를 기준으로 쓴다.

## 모든 변경에 공통

1. `npm run lint` 통과 (경고 새로 추가 금지)
2. `npm run build` 성공
3. 별도 브랜치에서 작업, main 직접 푸시 금지
4. 커밋 메시지: `feat|fix|docs|refactor|learn: <요약>` (한국어 요약 권장)

## 기능·버그 수정 추가 기준

5. **원인 진단 먼저**: 버그는 원인을 특정한 뒤 고친다. 증상만 가리는 수정 금지.
6. **실행 증거**: 개발 서버(`npm run dev`)에서 해당 시나리오를 실제로 수행한
   결과(로그, 스크린샷, 또는 단계별 확인 기록)를 PR에 첨부.
7. **회귀 확인**: 변경이 건드린 흐름의 인접 시나리오를 최소 1개 함께 검증
   (예: 저장 수정 → 새 게임 시작·전투 진입·재시작 후 복원 3가지 확인).
8. **게임 규칙 변경 시** docs/DESIGN.md 동기화, 세계관 텍스트는 docs/LORE*.md 준수.

## 완료 보고(PR) 필수 항목

- 대응하는 작업 ID와 제안 이슈 링크
- 변경 요약 (파일 단위)
- 위 게이트별 결과 (통과/실패/해당 없음)
- Claude 자체 검증 내용, 남은 문제·후속 제안
- 병합은 대표만 한다 (별도 승인)

## 판정 불가 시

게이트를 통과했는지 확신할 수 없으면 "통과"라고 쓰지 않는다.
실패·미검증을 그대로 보고하는 것이 규칙이다.
