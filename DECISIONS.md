# DECISIONS — 승인된 결정 기록

승인된 결정과 이유를 최신순으로 기록한다. 결정의 원본 근거(이슈/대화)가 있으면 링크.
이 파일은 감사 기록이다 — 과거 항목은 수정하지 않고, 번복은 새 항목으로 추가한다.

---

## 2026-09-01 — Development OS 분리 설계 확정, 파일럿 진행 승인 (대표 승인)

**결정**: 공통 개발 관제 시스템(Development OS)은 구운록에서 분리한다. 구운록은
관리 대상 프로젝트 중 하나다. 구조는 "GitHub = 공식 기록 원본, jh-server = 관제탑
(pm2 + Supabase 큐), Telegram(기존 nexus-tg-bot) = 대표 인터페이스, Cloud Routine =
보조(파일럿 미사용)". 본체는 별도 `development-os` 저장소, 승인함·텔레그램 어댑터는
Nexus(personal-os). 개발 에이전트가 관제 코드·승인 정책을 수정할 수 없도록 실행
권한도 분리한다. 이 대화 세션의 역할은 기획·검토·지시서 작성이고, 실제 코드 변경과
테스트는 승인 후 서버 CLI가 수행한다.

**확정 원칙 (요지)**:
- 제안 2유형(프로세스 개선 / 프로젝트 개선), 하루 **최대** 1개 — 근거 없거나 미처리
  제안 있으면 0개가 정상. 승인은 작업 ID + 제안 내용 해시에만 유효, 내용 변경 시 자동 무효.
- Codex 교차 검토는 필수 게이트. Codex 실패 시 "검토 대기/실패"로 기록·알림하고 승인
  가능 상태로 넘기지 않는다 (예외 진행은 대표 명시 승인 + 사유 기록).
- Claude·Codex 모두 실제 서버에서 제한 권한 모드로 시험 후 확정 (코드 존재 ≠ 검증).
  Codex 인증은 기존 서버 인증 확인 → 인증 방식·예산 결정 순서.
- 컨텍스트 효율 정책: 작업 연속성 기준 세션 수명, 역할별 Context Pack(이전 단계 대화
  전체 전달 금지), Context Builder는 결정적 조립기로 시작, CLAUDE.md 최소화, quiet
  출력(종료 코드·실행/실패 수·전체 로그 위치는 항상 기록), 서브에이전트는 총사용량·
  완료 시간·재작업 횟수로 사후 평가. 측정 못 한 값은 `미수집`으로 기록 (0 금지).
- 읽기 범위: 저장소 탐색 허용하되 비밀·인증 파일 제외. 체크포인트 재개는 실제 파일
  대조 검증 필수. 오케스트레이터에 bypassPermissions 금지.

**파일럿(Stage 0) 범위**: 조사 → 제안 → Claude/Codex 교차 검토 → Telegram 발송 →
승인 기록까지만. 관제 시스템 자체는 승인 후 서버 CLI가 구현하고, 관제 시스템이
제안한 개발 작업(구운록 포함)은 파일럿에서 구현하지 않는다. 변경 허용 범위:
구운록 제품 코드 변경 없음 / Nexus는 승인 어댑터·사전 승인 스키마만 / development-os는
파일럿 관제 기능만.

**진행 순서**: ① 서버 현황 확인(읽기 전용, 설치·변경 없음) → ② 결과 보고 후 대표가
설치·DB 변경·파일럿 배포 범위 승인 → ③ 구현. 목표 지표는 토큰 최소화가 아니라
"대표 개입·재작업을 줄이며 승인된 결과를 안정적으로 만드는 것".

**부수 결정**: 구운록 클라우드 루틴 2개는 일시정지 유지(파일럿 검증 후 삭제),
`claude/auto-learning-dev-process-shrrwf` 브랜치는 main에 병합하지 않음, 이슈 #1은
`architecture-pending` 보류(파일럿 후 새 프로토콜로 재상정). 이슈 #2는 도구 오조작
중복으로 닫음(감사 기록 유지).

## 2026-08-27 — AI 개발팀 구조 Stage 0 도입 (대표 승인)

**결정**: Claude(실행)·Codex(독립 검수)·대표(최종 승인)로 구성된 승인 기반 개발
구조를 도입한다. 이번 단계에서는 **제안까지만 자동화**하고 코드 수정·병합·배포는
하지 않는다.

**대표가 명시한 조건 (전문)**:
1. 거버넌스 문서 6종과 매일 개발 제안 루틴을 만든다.
2. 매일 제안은 현재 최우선 목표에 직접 기여하는 작업 최대 1개만 올린다.
3. 초기 단계에서는 위험도와 관계없이 대표 승인 전에는 어떤 개발도 시작하지 않는다.
4. GitHub 이슈를 임시 승인 인터페이스이자 영구 감사 기록으로 사용한다.
5. 승인은 작업 ID가 포함된 명확한 승인으로 처리하고, 제안 내용이 바뀌면 기존 승인은
   무효로 한다.
6. 기존 텔레그램 개발 봇과 서버 구조를 먼저 조사해서, 새 봇을 만들지 않고 승인 명령과
   Claude Routine 트리거를 붙일 수 있는지 검토한다. 텔레그램을 최종 사용자
   인터페이스로 유지한다.
7. Codex는 우선 PR 독립 검수에 연결하고, 기획 단계에서는 Claude Code용 Codex 플러그인
   호출 가능 여부를 작은 연결 시험으로 확인한다. 검증 전에는 GitHub 이슈 자동 검토가
   된다고 가정하지 않는다.
8. 첫 루틴은 예약 전에 Run now로 수동 실행해 제안 품질과 변경 범위를 검증한다.
9. 이번 단계에서는 제안까지만 자동화하고 코드 수정·병합·배포는 하지 않는다.

## 2026-08-27 — 자동 학습 지식 베이스 도입 (대표 승인)

**결정**: 예약 루틴이 매일 03:00 KST에 리서치를 돌려 `.claude/knowledge/`에 지식을
쌓고(`claude/knowledge-base` 브랜치), 개발 세션은 작업 전 이를 읽는다.
**이유**: 대표 부재 시에도 최신 기술·디자인 지식을 축적해 개발 품질을 높이기 위해.
